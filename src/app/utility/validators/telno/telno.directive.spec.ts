import { TelnoDirective } from './telno.directive';

describe('TelnoDirective', () => {
  it('should create an instance', () => {
    const directive = new TelnoDirective();
    expect(directive).toBeTruthy();
  });
});
