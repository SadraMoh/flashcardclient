import { CacheloadDirective } from './cacheload.directive';

describe('CacheloadDirective', () => {
  it('should create an instance', () => {
    const directive = new CacheloadDirective();
    expect(directive).toBeTruthy();
  });
});
